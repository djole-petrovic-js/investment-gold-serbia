/** @type {import('next').NextConfig} */
const nextConfig = {
  // debuggg - ovo bi trebalo sad da se skloni jel? ne treba mi vise.
  images: {
    domains: ['otkup.gvs-srbija.rs']
  },
}

// module.exports = nextConfig

// debuggg - ovo mozda ne radi, proveriti
export default nextConfig